# SerialMemory MCP Configuration
SERIALMEMORY_ENDPOINT=https://api.serialmemory.dev
SERIALMEMORY_API_KEY=sm_YOUR_API_KEY_HERE

# Optional: Bearer token for HTTP transport authentication
# If set, all HTTP requests must include Authorization: Bearer <token>
# If unset, HTTP transport allows unauthenticated access (local dev only)
MCP_HTTP_TOKEN=

# Optional: HTTPS certificate password (if using custom cert)
CERT_PASSWORD=

# Production TLS: Use a reverse proxy (nginx/caddy) for TLS termination.
# The built-in HTTPS dev cert is for localhost development only.
